{
  "extends": ["../../.eslintrc.json"],
  "plugins": ["react-hooks"],
  "ignorePatterns": ["ios/", "android/"],
  "rules": {
    "no-console": "error",
    "no-restricted-imports": [
      "error",
      {
        "paths": [
          {
            "name": "react-native",
            "importNames": ["TouchableOpacity"],
            "message": "Please avoid using TouchableOpacity from 'react-native'. Use our custom GalleryTouchableOpacity component instead."
          }
        ]
      }
    ]
  },
  "overrides": [
    {
      "files": ["metro.config.js", "tailwind.config.js"],
      "rules": {
        // Okay to use require in these files
        "@typescript-eslint/no-var-requires": "off"
      }
    }
  ]
}
