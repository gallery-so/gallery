export enum Directions {
  LEFT,
  RIGHT,
  UP,
  DOWN,
}

export enum NftMediaType {
  IMAGE,
  VIDEO,
  ANIMATION,
  AUDIO,
  MODEL,
}

export enum DisplayLayout {
  GRID = 'GRID',
  LIST = 'LIST',
}
